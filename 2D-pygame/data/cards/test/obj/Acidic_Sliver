name = 'Acidic Sliver'
cardnum = 138
type = characteristic('Creature')
supertype = characteristic('')
subtypes = characteristic(['Sliver'])
cost = 'BR'
color = characteristic(['B','R'])
text = ['All Slivers have "2, Sacrifice this permanent: This permanent deals 2 damage to target creature or player."']

subrole = Creature(2,2)
in_play_role = Permanent(card, subrole)


ability = ActivatedAbility(card, cost=MultipleCosts([ManaCost(2), SacrificeCost()]),
               target=Target(target_types=isCreatureOrPlayer),
	       effects=DealDamage(2))

subrole.static_abilities = [PermanentTrackingAbility(card,
                               enter_condition = isCreature.with(lambda c: c.subtypes == "Sliver"),
                               leave_condition = isCreature.with(lambda c: c.subtypes == "Sliver"),
                               effects = AddActivatedAbility(ability, expire=False)),
]
