name = 'Pacifism'
cardnum = 31
type = characteristic('Enchantment')
supertype = characteristic('')
subtypes = characteristic(['Aura'])
cost = '1W'
color = characteristic(['W'])
text = ['Enchant creature', "Enchanted creature can't attack or block."]

target_type = isCreature
out_play_role.abilities = [CastPermanentSpell(card, cost,
                               target=Target(target_types=target_type),
                               effects=AttachToPermanent())]
subrole = Aura(target_type)

in_play_role = Permanent(card, subrole)

#################################

subrole.abilities = []
subrole.triggered_abilities = []
subrole.static_abilities = []
subrole.attached_abilities = [AuraStaticAbility(card,
                                        effects=GiveKeyword(lambda role: override(role, "canAttack", lambda self: False, chain=True), keyword="Can't attack", expire=False)),
                                 AuraStaticAbility(card,
                                        effects=GiveKeyword(lambda role: override(role, "canBlock", lambda self: False, chain=True), keyword="Can't block", expire=False))
]

#################################
